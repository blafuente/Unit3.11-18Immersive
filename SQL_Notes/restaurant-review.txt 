Write a database schema: create a restaurant_db.sql file, or do things in teh query line. 
table called "restaurant". This table will contain the following information, 
you will use the appropriate data types to represent the info. Please use an auto-incrementing ID:

CREATE TABLE restaurant(
    id INT AUTO_INCREMENT PRIMARY KEY NOT NULL,
    name VARCHAR(50),
    distance INT(10),
    stars INT(1),
    category VARCHAR(50),
    favoriteDish VARCHAR(50),
    takeout INT(1),
    lastVisit DATE
);

